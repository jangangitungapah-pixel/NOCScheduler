name: Quality

on:
  push:
    branches: [main]
  pull_request:

# Temporary write permission is used only while WP-F00 bootstraps the first
# reviewed lockfile and exact Prettier output. It is removed immediately after.
permissions:
  contents: write

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: F00 quality gate
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm and Node.js
        uses: pnpm/setup@v2
        with:
          version: 11.17.0
          runtime: node@24.18.0
          install: false
          cache: ${{ hashFiles('pnpm-lock.yaml') != '' }}

      - name: Install dependencies
        shell: bash
        run: |
          if [[ -f pnpm-lock.yaml ]]; then
            pnpm install --frozen-lockfile
          else
            pnpm install --no-frozen-lockfile
          fi

      - name: Apply canonical formatting during F00 bootstrap
        run: pnpm format

      - name: Static and application quality gates
        run: pnpm quality

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Playwright smoke
        run: pnpm e2e:smoke

      - name: Commit reviewed F00 bootstrap outputs
        if: ${{ success() && github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pnpm-lock.yaml src/app/globals.css src/app/page.tsx

          if git diff --cached --quiet; then
            echo "No bootstrap outputs need committing."
            exit 0
          fi

          git commit -m "chore: commit F00 generated baseline"
          git push origin HEAD:main
