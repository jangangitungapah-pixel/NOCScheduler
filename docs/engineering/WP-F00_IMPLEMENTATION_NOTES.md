# WP-F00 — Project Setup & Engineering Baseline — Implementation Notes

> Status: **ACCEPTED**  
> Workplan: `WP-F00`  
> PRD anchors: PRD-14, PRD-16, PRD-19, PRD-20  
> Acceptance transition: user explicitly approved continuation to WP-F01

## Scope implemented

- Next.js App Router + React + TypeScript strict baseline.
- Node.js 24 and pnpm 11 runtime contract.
- ESLint flat config + Prettier deterministic formatting.
- `@/*` path alias and modular-monolith ownership documentation.
- Zod runtime environment validation and safe `.env.example`.
- Vitest + Testing Library baseline tests.
- Playwright Chromium smoke test.
- Tailwind CSS PostCSS integration.
- Initial semantic CSS variable infrastructure, expanded by WP-F01.
- Liveness route at `/api/health/live`; DB-aware readiness remains a later production/data phase.
- GitHub Actions quality workflow.
- Repository contribution conventions.
- pnpm 11 dependency-build security policy with version-scoped `allowBuilds` approvals.
- Committed `pnpm-lock.yaml` generated by the pinned toolchain after a successful full bootstrap gate.
- Final CI uses read-only repository permission and frozen dependency installation.
- Windows non-Administrator Corepack invocation path documented and verified.
- Cross-platform formatting/workspace-root/dev-origin issues discovered during user verification were repaired before phase acceptance.

## Dependency build policy

pnpm 11 uses `allowBuilds` as the canonical dependency lifecycle-script policy and fails installation when an unreviewed dependency requires a build script.

WP-F00 explicitly permits only the currently reviewed baseline versions:

- `sharp@0.34.5`
- `unrs-resolver@1.12.2`

The allow-list is intentionally version-scoped. A future dependency version change must be reviewed rather than inheriting script execution permission silently. `dangerouslyAllowAllBuilds` is not enabled.

Project-level pnpm settings are kept in `pnpm-workspace.yaml`, including strict engine and peer-dependency enforcement.

## Intentional boundaries at acceptance

WP-F00 did **not** implement product design-system components, application navigation shell, authentication, PostgreSQL/Drizzle schema, scheduling/payroll business logic, or production deployment topology.

Those responsibilities remain assigned to later workplan phases. WP-F01 began only after explicit user approval.

## Verification history

The bootstrap pipeline exposed and resolved setup issues instead of bypassing them:

1. unreviewed dependency build scripts were rejected,
2. pnpm 11 build approval policy was migrated to `allowBuilds`,
3. Prettier canonical output was applied,
4. a stale Vitest config override was removed rather than type-cast around,
5. bootstrap run `31604948474` passed formatting, lint, typecheck, Vitest, production build, Chromium installation, and Playwright smoke,
6. GitHub Actions committed the generated lockfile as commit `4afbe506af4c42599ab6bb6b3b5ac915e9386acb`,
7. temporary CI write permissions and bootstrap mutation steps were removed,
8. read-only run `31605277262` passed frozen install, formatting, lint, typecheck, Vitest, production build, Chromium installation, and Playwright smoke,
9. Windows local verification exposed Corepack shim permissions, CRLF formatting, Turbopack root detection, dev-origin, and generated `next-env.d.ts` handling; those issues were repaired,
10. final F00 repair run `31607360161` passed the complete read-only quality gate.

## Exit-gate verification

- [x] committed `pnpm-lock.yaml`,
- [x] clean frozen install on read-only workflow,
- [x] format check green,
- [x] lint green,
- [x] typecheck green,
- [x] Vitest green,
- [x] production build green,
- [x] Playwright smoke green,
- [x] no real secret intentionally committed,
- [x] Windows non-Administrator development path documented,
- [x] user accepted phase and requested WP-F01 continuation.

## Handoff

WP-F00 is accepted. Historical F00 notes remain here as the setup baseline; active implementation status is tracked in the newest phase notes and repository README.
