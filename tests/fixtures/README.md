# Test Fixtures

Deterministic typed fixture sets will be added as product surfaces and domains are implemented.

Rules:

- never depend on production data,
- use stable identifiers and controlled dates,
- keep timezone assumptions explicit (`Asia/Jakarta`),
- prefer reusable scenario builders over copy-pasted fixture objects.
