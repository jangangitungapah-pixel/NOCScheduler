# Components

Shared UI and layout components belong here.

- `ui/` is introduced in WP-F01 for design-system primitives.
- `layout/` is introduced in WP-F02 for application shell composition.

Pages must prefer shared semantic components over ad-hoc local copies when a reusable primitive exists.
